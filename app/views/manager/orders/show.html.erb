<main>
  <div class="py-5 text-center">
    <h4 class="mb-3 h2">Order №<%=@order.id%></h4>
    
  </div>
  <%=@product%>
  <div class="row g-5">
    <div class="bg-body-tertiary col-md-5 col-lg-4 order-md-last" style="border-radius:20px;background:#6d757d;">
      <%= form_for @order do |form| %>

        <div>
          <%= form.label :status %><br>
          <%= form.select :status, ["new", "in progress", "completed"], selected: 'public' %>
        </div>
        <hr class="my-4">
        <%= form.submit "Save changes ", class:"w-100 btn btn-primary btn-lg" %>

      <% end %>
    </div>
    

    <div class="col-md-7 col-lg-8">
      
      
      <div class="col-12 d-flex flex-column">
        <div class="col-11 d-flex justify-content-between">
          <p class="col-2">
            Product name
          </p>
          <p class="col-1">
            Price
          </p>
          <p class="col-2">
            Count
          </p>
          <p class="col-1">
            sum
          </p>
        </div>

        <% @order_products.each do |order_product| %>
          <div class="col-11 d-flex justify-content-between bg-body-tertiary">
            <p class="col-2">
              <!--<%= order_product.product.image %>-->
              <%= order_product.product_name %><br>
            </p>
            <p class="col-1">
              <%= order_product.product_price %>$
            </p>
            <p class="text-body-secondary col-2">
              <%= order_product.product_count %>
            </p>
            <p class="col-1">
              <%= order_product.product_price * order_product.product_count %>$
            </p>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</main>